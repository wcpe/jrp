# 统一以 LF 检出文本文件，保证 gofmt、Prettier 等检查在 Windows 上不会
# 因检出 CRLF 而失败，同时消除跨平台的行尾抖动。
* text=auto eol=lf

# 二进制资源不做行尾转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
*.woff binary
*.woff2 binary
*.ttf binary
*.eot binary

# Windows 专用脚本保留 CRLF，避免批处理解释器解析异常
*.bat text eol=crlf
*.cmd text eol=crlf
