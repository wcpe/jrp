# 安全说明：JRP

> 本文定义 JRP 的信任模型、敏感数据处理、外部输入校验和漏洞报告要求。安全模型变化时必须同步更新。

## 1. 信任模型

JRP P1 面向个人与小团队的单管理员、单机部署。`jrps` 数据目录、`jrpc` 数据目录、运行账户和宿主机是明确的信任边界；浏览器、官方 frpc、jrpc、被代理服务、互联网客户端及所有网络输入均不默认可信。

JRP 不承诺抵御以下场景：

- 本机管理员或可读取 JRP 数据目录的账户主动访问数据。
- 磁盘、备份介质或虚拟机快照泄露。
- 宿主机被入侵后读取进程内存、SQLite、日志或正文分段文件。
- P1 的 HTTPS 透传流量被解释为可读取正文；未由 JRP 终止 TLS 时，JRP 不采集其 HTTP 正文。

部署者必须使用专用低权限账户运行进程，限制数据目录和备份文件权限，并保护管理入口不暴露到不可信网络。

## 2. 明文凭证与正文风险

根据项目已接受的产品选择，客户端 token、管理员会话相关数据、配置中的凭证和采集到的 HTTP 正文均可能以明文形式静态存储。JRP P1 **不提供静态加密保护**，不得在文档、UI 或运维说明中声称这些数据已加密。

这意味着一旦数据目录、SQLite、正文分段文件或备份被读取，相关凭证和业务内容可能直接泄露。降低风险的强制措施如下：

- 凭证不得硬编码到源码、测试、提交信息或仓库配置。
- API、Web、日志和错误响应不得回显完整 token、密码、Cookie、Authorization 或正文敏感字段。
- 客户端 token 必须按客户端隔离，支持轮换和吊销，禁止所有客户端共享单一 token。
- HTTP 正文采集默认关闭，仅允许管理员逐代理开启。
- 正文查看、导出和删除必须记录审计事件；审计记录不得包含完整正文或完整凭证。
- 默认保留期为 30 天，正文压缩分段文件总量上限为 5 GiB，达到任一条件时按既定清理策略回收。
- 备份与生产数据采用相同或更严格的文件权限和保留策略。

## 3. 传输与会话

- 管理 API 和 jrpc 管理通道使用 HTTPS/WSS；生产部署不得使用明文管理连接。
- 首次连接目标支持自签名证书与指纹固定，证书更换必须经显式信任更新。
- 管理 Web 使用安全 Cookie 会话和 CSRF 防护；修改操作不得仅依赖 Cookie 而无 CSRF 校验。
- 数据隧道遵循 frpc 兼容协议。JRP 私有管理消息不得混入官方兼容数据协议。
- 登录、心跳、工作连接和访客请求必须校验时间窗口、鉴权材料、连接归属与重放边界。

## 4. 外部输入

所有来自管理 API、WebSocket、SSE、官方 frpc、jrpc、代理用户、Webhook 目标和配置数据库的数据在进入业务边界前都必须校验：

- 限制长度、数量、枚举、端口、地址、域名、路径和帧大小。
- 对未知消息、畸形帧、超大正文、非法状态迁移和重复请求采用可预测的拒绝策略。
- 数据库操作使用参数化查询或 ORM 参数绑定，不拼接不可信 SQL。
- Web 输出按上下文转义；Webhook 和代理目标必须防止 SSRF、路径穿越和开放重定向。
- 正文采集必须设置内存、磁盘、并发和超时上限，禁止在请求热路径无界缓冲。
- 错误对外使用稳定问题码，不暴露堆栈、文件路径、SQL、内部地址或密钥。

## 5. 权限与审计

P1 只有一个管理员，不实现多租户、RBAC 或 OIDC。单管理员不等于无授权检查：所有管理端点仍须验证会话与 CSRF，jrpc 只能访问属于自身的 desired state。

以下操作必须进入审计：登录与登出、token 创建/轮换/吊销、配置变更、配置应用结果、正文查看/导出/删除、通知目标变更和保留策略变更。审计事件应包含时间、主体、动作、对象和结果，不记录完整秘密。

审计事件的完整性口径：

- **不可改写**：审计事件写入后不允许就地修改。该约束由数据库触发器强制，不依赖应用层自觉。
- **可按保留策略清理**：审计按独立于正文的保留策略（默认 180 天）只按时间维度清理，不按容量。清理动作本身先写入一条审计事件再执行删除，因此"清理发生过"始终有据可查。
- **不因正文清理而删除**：正文的 30 天 / 5 GiB 清理不牵连审计事件；反之审计清理也不影响正文。
- **不提供抹除通道**：管理 API 不提供审计导出与删除接口，管理员无法经 API 抹除审计记录。

审计写入与业务结果同事务提交：审计写入失败即整体回滚，不留下"发生了但没有记录"的操作。审计内容不得包含完整 token、密码、Cookie、Authorization、完整正文或分段文件路径。

## 6. 依赖与供应链

- Core 不得依赖 `github.com/fatedier/frp` 或导入参考仓库代码。
- Go 与前端依赖必须锁定并接受静态检查、漏洞扫描和许可证检查。
- 构建与发布流程不得把 `.env`、SQLite、正文分段、日志、覆盖率或本地凭证打入产物。

## 7. 漏洞报告

请通过仓库 `github.com/wcpe/jrp` 的 GitHub Security Advisory 私下报告安全问题；在维护者完成评估和修复前，不要公开漏洞细节或可利用样例。

报告应包含受影响版本、影响范围、复现条件、最小复现、潜在缓解措施以及是否已公开。维护者应先确认收到，再协调修复与披露时间；普通缺陷不得包含真实凭证、正文或个人数据。
