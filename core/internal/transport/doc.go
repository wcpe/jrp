// Package transport 把一条 TCP 连接抽象成 Core 可组合的字节流资源（FR-05a）。
//
// 只使用 Go 标准库，不引入任何第三方依赖。职责边界：
//   - 监听句柄的接管与释放，以及 Accept 错误的临时/致命分类。
//   - 带超时的拨号，禁止无超时拨号。
//   - 连接句柄：附带用途标记（控制/工作）、代理归属与建链时间。
//   - 工作连接池的上限与槽位回收，以及暂存工作连接的空闲回收。
//   - 双向桥接与半关闭收尾。
//
// 本包不解释 wire 语义，不决定业务超时取值：全部超时与上限由配置快照提供。
package transport
